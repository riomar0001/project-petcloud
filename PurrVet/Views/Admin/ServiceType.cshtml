@model PurrVet.Models.ServiceTypeListViewModel

@{
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";
    ViewBag.Title = "Service Types";

    var categories = ViewBag.Categories as List<PurrVet.Models.ServiceCategory>;
    var selectedCategory = Model.CategoryFilter.HasValue
        ? categories?.FirstOrDefault(c => c.CategoryID == Model.CategoryFilter)?.ServiceType ?? "All"
        : "All";
    int maxPagesToShow = 5;
    int blockSize = 10;
    int startPage = Math.Max(1, Model.CurrentPage - 2);
    int endPage = Math.Min(Model.TotalPages, Model.CurrentPage + 2);
}

<div class="d-md-none px-3">
    <form method="get" class="d-flex align-items-center gap-2 mb-2">
        <input type="text" name="searchQuery" class="form-control" placeholder="Search..." value="@Model.SearchQuery" />
        <button type="submit" class="btn" style="background-color:#559DD4; color:white;">
            <i class="bi bi-search"></i>
        </button>
    </form>

    <button class="btn text-black w-100 fw-semibold" id="toggleFilter" style="background-color:#FFB100;">
        <i class="bi bi-funnel"></i> Filters
    </button>
</div>

<div id="filterSection" class="filter-mobile d-flex flex-wrap align-items-center justify-content-start px-3 py-2" style="background-color:#FFD392;">
    <span class="fw-semibold me-2">Sort by:</span>
    <div class="dropdown me-3">
        <button class="btn text-black dropdown-toggle" type="button" id="sortFieldBtn" data-bs-toggle="dropdown"
                style="background-color:#FFB100;">
            @(ViewBag.SortField switch
                {
                    "id" => "Subtype ID",
                    "name" => "Service Name",
                    "desc" => "Description",
                    _ => "Subtype ID"
                })
        </button>
        <ul class="dropdown-menu">
            <li><a class="dropdown-item text-black sort-field" data-field="id" href="#">Subtype ID</a></li>
            <li><a class="dropdown-item text-black sort-field" data-field="name" href="#">Service Name</a></li>
            <li><a class="dropdown-item text-black sort-field" data-field="desc" href="#">Description</a></li>
        </ul>
    </div>

    <div class="dropdown me-3">
        <button class="btn text-black dropdown-toggle" type="button" id="sortOrderBtn" data-bs-toggle="dropdown"
                style="background-color:#FFB100;">
            @(ViewBag.SortOrder == "desc" ? "Descending (↓)" : "Ascending (↑)")
        </button>
        <ul class="dropdown-menu">
            <li><a class="dropdown-item text-black sort-order" data-order="asc" href="#">Ascending (↑)</a></li>
            <li><a class="dropdown-item text-black sort-order" data-order="desc" href="#">Descending (↓)</a></li>
        </ul>
    </div>

    <form method="get" class="d-none d-md-flex justify-content-end flex-grow-1">
        <div class="input-group" style="max-width:300px;">
            <input type="text" name="searchQuery" class="form-control" placeholder="Search..."
                   value="@Model.SearchQuery" />
            <button type="submit" class="btn" style="background-color:#559DD4; color:white;">
                <i class="bi bi-search"></i>
            </button>
        </div>
    </form>
</div>
<div class="card shadow-sm border-0">
    <div class="card-body">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <h6 class="fw-bold mb-0">SERVICE TYPES</h6>
            <a href="~/Admin/AddServiceSubtype" class="btn text-white" style="background-color:#559DD4;">
                + Add Type
            </a>
        </div>

        <div class="table-responsive">
            <table class="table align-middle text-center border">
                <thead class="table-light">
                    <tr>
                        <th style="width:6%">ID</th>
                        <th>Subtype</th>
                        <th>Category</th>
                        <th>Description</th>
                        <th style="width:15%">Action</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.ServiceSubtypes != null && Model.ServiceSubtypes.Any())
                    {
                        foreach (var subtype in Model.ServiceSubtypes)
                        {
                            <tr>
                                <td>@subtype.SubtypeID</td>
                                <td>@subtype.ServiceSubType</td>
                                <td>@subtype.ServiceCategory?.ServiceType</td>
                                <td>@subtype.Description</td>
                                <td>
                                    <a href="~/Admin/EditServiceSubtype/@subtype.SubtypeID" class="btn btn-sm btn-edit">
                                        <i class="bi bi-pencil-fill"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="5" class="text-center text-muted">No records found.</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <nav class="mt-3">
            <ul class="pagination justify-content-center mb-0">

                <li class="page-item @(Model.CurrentPage == 1 ? "disabled" : "")">
                    <a class="page-link border-0 text-dark"
                       href="@(Model.CurrentPage == 1 ? "#" : $"?page={Model.CurrentPage - 1}&searchQuery={Model.SearchQuery}&categoryFilter={Model.CategoryFilter}&sortField={ViewBag.SortField}&sortOrder={ViewBag.SortOrder}")">
                        ‹
                    </a>
                </li>

                @if (startPage > 1)
                {
                    <li class="page-item">
                        <a class="page-link rounded-circle border-0 text-dark"
                           style="width:32px;height:32px;display:flex;align-items:center;justify-content:center;"
                           href="?page=1&searchQuery=@Model.SearchQuery&categoryFilter=@Model.CategoryFilter&sortField=@ViewBag.SortField&sortOrder=@ViewBag.SortOrder">1</a>
                    </li>

                    <li class="page-item">
                        <a class="page-link border-0 text-dark fw-bold"
                           href="?page=@(Math.Max(1, Model.CurrentPage - blockSize))&searchQuery=@Model.SearchQuery&categoryFilter=@Model.CategoryFilter&sortField=@ViewBag.SortField&sortOrder=@ViewBag.SortOrder">
                            ...
                        </a>
                    </li>
                }

                @for (int i = startPage; i <= endPage; i++)
                {
                    if (i == Model.CurrentPage)
                    {
                        <li class="page-item active">
                            <a class="page-link rounded-circle border-0 text-black"
                               style="background-color:#FFB100;width:32px;height:32px;display:flex;align-items:center;justify-content:center;"
                               href="#">
                                @i
                            </a>
                        </li>
                    }
                    else
                    {
                        <li class="page-item">
                            <a class="page-link rounded-circle border-0 text-dark"
                               style="width:32px;height:32px;display:flex;align-items:center;justify-content:center;"
                               href="?page=@i&searchQuery=@Model.SearchQuery&categoryFilter=@Model.CategoryFilter&sortField=@ViewBag.SortField&sortOrder=@ViewBag.SortOrder">
                                @i
                            </a>
                        </li>
                    }
                }

                @if (endPage < Model.TotalPages)
                {
                    <li class="page-item">
                        <a class="page-link border-0 text-dark fw-bold"
                           href="?page=@(Math.Min(Model.TotalPages, Model.CurrentPage + blockSize))&searchQuery=@Model.SearchQuery&categoryFilter=@Model.CategoryFilter&sortField=@ViewBag.SortField&sortOrder=@ViewBag.SortOrder">...</a>
                    </li>

                    <li class="page-item">
                        <a class="page-link rounded-circle border-0 text-dark"
                           style="width:32px;height:32px;display:flex;align-items:center;justify-content:center;"
                           href="?page=@Model.TotalPages&searchQuery=@Model.SearchQuery&categoryFilter=@Model.CategoryFilter&sortField=@ViewBag.SortField&sortOrder=@ViewBag.SortOrder">
                            @Model.TotalPages
                        </a>
                    </li>
                }

                <li class="page-item @(Model.CurrentPage == Model.TotalPages ? "disabled" : "")">
                    <a class="page-link border-0 text-dark"
                       href="@(Model.CurrentPage == Model.TotalPages ? "#" : $"?page={Model.CurrentPage + 1}&searchQuery={Model.SearchQuery}&categoryFilter={Model.CategoryFilter}&sortField={ViewBag.SortField}&sortOrder={ViewBag.SortOrder}")">
                        ›
                    </a>
                </li>

            </ul>
        </nav>
    </div>
</div>

<script>
    $(function () {
        let sortField = '@(ViewBag.SortField ?? "id")';
        let sortOrder = '@(ViewBag.SortOrder ?? "asc")';
        const searchQuery = '@(Model.SearchQuery ?? "")';

        $('.sort-field').on('click', function (e) {
            e.preventDefault();
            sortField = $(this).data('field');
            reloadPage();
        });

        $('.sort-order').on('click', function (e) {
            e.preventDefault();
            sortOrder = $(this).data('order');
            reloadPage();
        });

        function reloadPage() {
            const url = `?searchQuery=${encodeURIComponent(searchQuery)}&sortField=${encodeURIComponent(sortField)}&sortOrder=${encodeURIComponent(sortOrder)}`;
            window.location.href = url;
        }
    });
    document.addEventListener("DOMContentLoaded", function () {
        const toggleBtn = document.getElementById("toggleFilter");
        const filterSection = document.getElementById("filterSection");
        if (toggleBtn && filterSection) {
            toggleBtn.addEventListener("click", function () {
                filterSection.classList.toggle("active");
            });
        }
    });
</script>
